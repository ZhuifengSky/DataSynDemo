<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:batch="http://www.springframework.org/schema/batch" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/batch
        http://www.springframework.org/schema/batch/spring-batch.xsd
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans.xsd
    ">

	<!-- <import resource="../applicationContext_batch.xml" /> -->

	<bean id="student" class="com.main.modules.student.modelmap.Student"
		scope="prototype" />
	<bean id="studentMapper" class="com.main.modules.student.modelmap.StudentMapper"></bean>
	<bean id="studentProcessor" class="com.main.modules.student.processor.StudentProcessor" />

	<!-- reader 配置-->
	<bean id="studentReader"
		class="org.springframework.batch.item.database.JdbcCursorItemReader" scope="step">
		<property name="dataSource" ref="sourceDataSource" />
		<!--Sql语句 -->
		<property name="sql" value="SELECT user_type,ACCOUNT_NO FROM T_STUDENT" />
		<!--需要一个映射类 -->
		<property name="rowMapper" ref="studentMapper" />
	</bean>

	<!-- Writer 配置 -->
	<bean id="studentWriter"
		class="org.springframework.batch.item.database.JdbcBatchItemWriter">
		<property name="dataSource" ref="targetDataSourceServer" />
		<property name="sql"
			value="insert into t_student(studentname,age)
	    	values(:studentName,:age)" />
		<property name="itemSqlParameterSourceProvider">
			<bean class="org.springframework.batch.item.database.BeanPropertyItemSqlParameterSourceProvider" />
		</property>
	</bean>
	<!-- Job 配置  -->
	<batch:job id="synStudent">
		<batch:step id="step1">
			<batch:tasklet>
				<batch:chunk reader="studentReader" writer="studentWriter"
					processor="studentProcessor" commit-interval="2">
				</batch:chunk>
			</batch:tasklet>
		</batch:step>
	</batch:job>
	
</beans>